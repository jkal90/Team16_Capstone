<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.msu.huangmax.cse480_draft1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CSE480_DRAFT1">
        <activity android:name=".ThankYouActivity"></activity>
        <activity android:name=".WakeUpQuestionsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RepeatingActivity" />
        <activity android:name=".StartTimerActivity" />

        <receiver android:name=".NotificationReceiver" />
    </application>

</manifest>